<img src="{{ asset('images/logo.png') }}" {{ $attributes }}>
    <style>
        .logo-text {
            font-family: Figtree, sans-serif;
            font-size: 20px;
            font-weight: 800;
            letter-spacing: -0.5px;
        }
    </style>
</img>